# hello-js

A minimal JavaScript starter project.

## Quick start

Open a PowerShell terminal in the project root and run:

```powershell
npm install
npm test
```

Run the app:

```powershell
npm start -- "Your Name"
```

Lint and format:

```powershell
npm run lint
npm run format
```
